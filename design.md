# 設計ドキュメント

## 要件定義と決定理由

### 扱う商品とデータの一元管理
システム内で扱う商品は、以下の3種類としました。
- `💧 水` : 120円
- `🍵 お茶` : 150円
- `☕ コーヒー` : 130円

決定理由:
日本の一般的な自動販売機で最も標準的かつ購入頻度の高いラインナップをモデルとしました。価格設定を「10円単位（端数あり）」にすることで、単純な100円均一の処理ではなく、JavaScript側で正確な引き算（残高不足の判定や、おつり計算）が行われているかを厳格にテストできるようにしました。

### 投入金額の制限ルール
ユーザーからの入力（投入金額）に対して、以下の制限を設けました。
- 小数の入力禁止（例: 100.5円など）
- 文字列や0円以下の入力禁止
- 1円玉・5円玉の投入禁止（10円単位のみ受付）

決定理由:
現実の自動販売機の物理的な硬貨識別センサーの挙動を模倣するためです。プログラム的な例外エラー（バグ）を防ぐだけでなく、日本の一般的な自販機が「10円玉、50円玉、100円玉、500円玉、1000円札」のみを受け付けるという商習慣をロジック（余り算: `%`）で表現しました。

## データ構造と関数分割

### データ構造の選定
商品情報は、HTML側に直接書き込む（ハードコーディングする）のではなく、JavaScriptの「オブジェクト配列（Array of Objects）」として一元管理しました。

### 関数分割の方針
コードを作成する際、機能ごとに方針を考え一つの関数ごとに役割を持つように開発を進めました。